import TicketCreator from "@/features/tickets/components/TicketCreator";

const CreateTicket = () => {
  return (
    <div>
      <main>
        <TicketCreator />
      </main>
    </div>
  );
};

export default CreateTicket;
