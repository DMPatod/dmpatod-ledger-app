import TicketsTable from "@/features/tickets/components/TicketsTable";

const IndexTicket = () => {
  return (
    <div>
      <main>
        <TicketsTable />
      </main>
    </div>
  );
};

export default IndexTicket;
