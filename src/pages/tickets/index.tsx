import TicketsTable from "@/features/tickets/components/TicketsTable";

const IndexTickets = () => {
  return (
    <div>
      <main>
        <TicketsTable />
      </main>
    </div>
  );
};

export default IndexTickets;
